@model dynamic
@{
    ViewBag.Title = "Tiquete Producto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>@ViewBag.Title</h1>
<section id="contact" class="contact-section contact-style-1 mb-80">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="contact-form-wrapper">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a1">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 1)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a2">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 2)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a3">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 3)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a4">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 4)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a5">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 5)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a6">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 6)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a7">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 7)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a8">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 8)
                                        {
                                            <option value="@item.NombreProducto"></option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <input type="submit" value="Aprobar" class="button button-sm radius-10 btn-bloc"
                               onclick="ActualizarTiqueteJS();">
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="contact-image">
        <img src="~/images/form/illustation.svg" alt="">
    </div>
</section>
