@model dynamic
@{
    ViewBag.Title = "Tiquete Producto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>@ViewBag.Title</h1>
<section id="contact" class="contact-section contact-style-1 mb-80">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="contact-form-wrapper">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a1">
                                    @foreach (var item in Model.cp)
                                    {
                                        if (item.CategoriaProductoID == 1)
                                        {
                                            <option value="@item.ProductoID">@item.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a2">
                                    @foreach (var item2 in Model.cp)
                                    {
                                        if (item2.CategoriaProductoID == 2)
                                        {
                                            <option value="@item2.ProductoID">@item2.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a3">
                                    @foreach (var item3 in Model.cp)
                                    {
                                        if (item3.CategoriaProductoID == 3)
                                        {
                                            <option value="@item3.ProductoID">@item3.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a4">
                                    @foreach (var item4 in Model.cp)
                                    {
                                        if (item4.CategoriaProductoID == 4)
                                        {
                                            <option value="@item4.ProductoID">@item4.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a5">
                                    @foreach (var item5 in Model.cp)
                                    {
                                        if (item5.CategoriaProductoID == 5)
                                        {
                                            <option value="@item5.ProductoID">@item5.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a6">
                                    @foreach (var item6 in Model.cp)
                                    {
                                        if (item6.CategoriaProductoID == 6)
                                        {
                                            <option value="@item6.ProductoID">@item6.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a7">
                                    @foreach (var item7 in Model.cp)
                                    {
                                        if (item7.CategoriaProductoID == 7)
                                        {
                                            <option value="@item7.ProductoID">@item7.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="single-input">
                                <label for="presupuesto">Presupuesto</label>
                                <select id="a8">
                                    @foreach (var item8 in Model.cp)
                                    {
                                        if (item8.CategoriaProductoID == 8)
                                        {
                                            <option value="@item8.ProductoID">@item8.NombreProducto</option>
                                        }
                                    }
                                </select>
                                <i class="lni lni-coin"></i>
                            </div>
                        </div>
                        @foreach (var item9 in Model.cta)
                        {
                            if (item9.TicketID == Model.TicketID.TicketID)
                            {
                                <input type="submit" value="Aprobar" class="button button-sm radius-10 btn-bloc"
                                       onclick="ActualizarTiqueteJS(@item9.Usuario_Empleado_Asig, @item9.Usuario_Cliente, 3,
                                       @item9.Presupuesto, '@item9.Detalles', @item9.TicketID);">
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="contact-image">
        <img src="~/images/form/illustation.svg" alt="">
    </div>
</section>
